# Meme Vault v0.4.2 Release Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Document, version, verify, commit, and push the completed waterfall-edit fix and AI suggested-title feature as Meme Vault v0.4.2.

**Architecture:** Keep historical milestone references unchanged while updating the current-version entry points and adding one completed v0.4.2 milestone. Treat the existing dirty working tree as the intentional release scope, verify it completely, create one release commit, and push the current branch without a tag, PR, or force push.

**Tech Stack:** Markdown, npm package metadata, native TypeScript/Vite/Vitest, Python/FastAPI/Pytest, Git.

## Global Constraints

- Release version is exactly `v0.4.2`; package metadata uses exactly `0.4.2`.
- Keep historical v0.4.1 milestone and design-plan references unchanged.
- Commit the existing waterfall-edit fix, AI suggested-title implementation, their tests, and release documents together.
- Continue on `codex/v0.4.1-batch-upload`.
- Commit message is exactly `release: v0.4.2`.
- Push only to the branch's existing `origin` upstream.
- Do not create a Git tag or Pull Request.
- Do not force-push.

---

### Task 1: Update current-version documentation and metadata

**Files:**
- Modify: `README.md:1-5`
- Modify: `docs/CODEBASE_STATUS.md`
- Modify: `docs/PROJECT_PLAN.md:290-378`
- Modify: `frontend/package.json:4`
- Modify: `frontend/package-lock.json:3-9`

**Interfaces:**
- Consumes: completed waterfall-edit and AI suggested-title behavior already present in the worktree
- Produces: consistent current version `v0.4.2` across user-facing documentation and npm metadata

- [ ] **Step 1: Update package metadata**

Change only the current package version fields:

```json
"version": "0.4.2"
```

Apply this to `frontend/package.json`, the root lockfile version, and `packages[""].version` in `frontend/package-lock.json`.

- [ ] **Step 2: Update README current-version copy**

Change the heading to:

```markdown
# Meme Vault v0.4.2
```

Change the overview to identify v0.4.2 and explicitly mention:

- Meme edits preserve loaded waterfall pages, order, pagination state, and scroll position through local replacement.
- AI group analysis now previews a suggested title that is only applied after explicit user confirmation.

- [ ] **Step 3: Update CODEBASE_STATUS implementation baseline**

Change the baseline line to:

```markdown
> 更新基线：v0.4.2 实现状态（2026-07-31）。本文描述已经落地的代码，不是下一阶段需求。
```

Update the current-capability and AI/data-flow sections to record:

- AI analysis returns a suggested title in addition to description, tags, and template.
- `meme_ai_analyses.suggested_title` is nullable for historical compatibility.
- `apply_title` defaults to false and only confirmed opt-in changes the Meme title.
- Successful Meme edits locally replace the matching `state.memes` entry and card rather than reloading the first page.
- Tag changes refresh only the tag filter list, preserving `offset`, `hasMore`, order, loaded count, scroll position, and unaffected card nodes.

Update the document's verification summary to the latest final counts from Task 2.

- [ ] **Step 4: Add the completed v0.4.2 project milestone**

Insert after v0.4.1:

```markdown
### v0.4.2：编辑稳定性与 AI 建议标题（已完成）

- [x] Meme 编辑成功后使用接口返回结果原地替换状态和对应卡片，不重新请求 Meme 第一页。
- [x] 保持已加载数量、顺序、offset、hasMore、滚动位置及其他卡片节点不变；标签变化时只刷新标签筛选列表。
- [x] AI 图片组分析生成一条简体中文建议标题，并保存在兼容历史记录的可空分析快照字段中。
- [x] “采用建议标题”默认关闭，只有用户勾选并确认后才更新 Meme 标题。
- [x] 补充后端、迁移、API 和前端回归测试，并同步项目版本与状态文档。

验收：加载两页后编辑不会再次调用 listMemes；AI 建议标题可预览、默认不采用、确认失败保留选择，旧分析记录继续可用。
```

Change only the current-status line to:

```text
当前状态：v0.4.2 编辑稳定性与 AI 建议标题已完成
```

- [ ] **Step 5: Verify version consistency without rewriting history**

Run:

```powershell
rg -n "v0\.4\.2|\"version\": \"0\.4\.2\"" README.md docs/CODEBASE_STATUS.md docs/PROJECT_PLAN.md frontend/package.json frontend/package-lock.json
rg -n "v0\.4\.1" docs/PROJECT_PLAN.md docs/superpowers
```

Expected: all current-version files contain v0.4.2/0.4.2, while the v0.4.1 milestone and its historical design/plan references still exist.

### Task 2: Run release verification and audit the commit boundary

**Files:**
- Verify: all modified and untracked release files

**Interfaces:**
- Consumes: version/document updates from Task 1 and the completed source/test changes
- Produces: fresh evidence that the exact pre-commit tree passes all required checks

- [ ] **Step 1: Run frontend type checking**

Run:

```powershell
npm.cmd --prefix frontend run typecheck
```

Expected: exit code 0.

- [ ] **Step 2: Run the complete frontend test suite**

Run:

```powershell
npm.cmd --prefix frontend test
```

Expected: all frontend tests pass with zero failures.

- [ ] **Step 3: Build the production frontend**

Run:

```powershell
npm.cmd --prefix frontend run build
```

Expected: exit code 0 and a successful Vite build.

- [ ] **Step 4: Run the complete backend test suite**

Run:

```powershell
python -m pytest -q
```

Expected: all backend tests pass with zero failures.

- [ ] **Step 5: Inspect formatting and scope**

Run:

```powershell
git diff --check
git status -sb
git diff --stat
git diff -- README.md docs/CODEBASE_STATUS.md docs/PROJECT_PLAN.md frontend/package.json frontend/package-lock.json
```

Expected:

- `git diff --check` exits 0.
- Only the completed waterfall-edit fix, AI title implementation/tests, their design/plan files, and v0.4.2 release files are present.
- No `frontend/dist`, database, media, cache, environment, or secret files are included.

### Task 3: Commit and push v0.4.2

**Files:**
- Stage: the complete verified release scope from Task 2

**Interfaces:**
- Consumes: verified working tree and existing upstream configuration
- Produces: one local commit and the same commit on `origin/codex/v0.4.1-batch-upload`

- [ ] **Step 1: Confirm branch and upstream immediately before staging**

Run:

```powershell
git branch --show-current
git rev-parse --abbrev-ref --symbolic-full-name "@{upstream}"
```

Expected:

```text
codex/v0.4.1-batch-upload
origin/codex/v0.4.1-batch-upload
```

- [ ] **Step 2: Stage the verified release scope**

Run:

```powershell
git add README.md docs frontend/package.json frontend/package-lock.json frontend/src app tests
git status --short
```

Inspect every staged path. If an unexpected generated, data, cache, environment, or secret file appears, stop before committing and remove it from the index without deleting the local file.

- [ ] **Step 3: Create the release commit**

Run:

```powershell
git commit -m "release: v0.4.2"
```

Expected: one commit is created successfully.

- [ ] **Step 4: Verify the committed tree and commit identity**

Run:

```powershell
git status -sb
git show --stat --oneline --decorate HEAD
```

Expected: working tree is clean and `HEAD` is the `release: v0.4.2` commit.

- [ ] **Step 5: Push without force**

Run:

```powershell
git push origin codex/v0.4.1-batch-upload
```

Expected: the remote branch advances to the local release commit. Do not add `--force`, create a tag, or create a Pull Request.

- [ ] **Step 6: Confirm local and upstream commits match**

Run:

```powershell
git fetch origin codex/v0.4.1-batch-upload
git rev-parse HEAD
git rev-parse origin/codex/v0.4.1-batch-upload
git status -sb
```

Expected: both commit hashes are identical and the branch is neither ahead nor behind.
